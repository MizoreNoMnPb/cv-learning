# Pointer: vision-language pretraining (not a DA method)

本夹只作导航。GLIP、RegionCLIP、GLIPv2、Grounding DINO 的原问题是开放词汇检测 / 区域–文本预训练，不是领域自适应。

- CLIP 串讲：[../../02-Vision-Tasks/clip.md](../../02-Vision-Tasks/clip.md)；PDF 在 `02-Vision-Tasks/Multimodal/paper/` 下该论文目录的 `paper.pdf`。
- Grounding DINO 的检测谱系见 `02-Vision-Tasks/DETR.md`（第一波并入 Faster R-CNN → DETR 串讲）。
- 用 VLM **做异常检测** 的方法族在 [`../../03-Anomaly-Detection/VLM-AD/`](../../03-Anomaly-Detection/VLM-AD/)，与本夹不是同一件事。
